import { View, Text } from 'react-native';
import React from 'react';

const ChatList = () => {
  return (
    <View className="">
      <Text>ChatList</Text>
    </View>
  );
};

export default ChatList;
